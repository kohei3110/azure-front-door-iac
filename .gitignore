# OS
.DS_Store
.AppleDouble
.LSOverride

# VS Code
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
*.code-workspace

# Node.js
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# dotenv (secrets)
.env
.env.*
!.env.example

# Logs
logs/
*.log

# Build / dist output
/dist/
/build/
/out/
.out/

# Coverage
coverage/
.nyc_output/

# Runtime
*.pid
*.seed
*.pid.lock

# Cache
.cache/
.eslintcache

# TypeScript (if used)
*.tsbuildinfo

# Azure / Bicep / ARM
# Keep source .bicep/.json; ignore local parameter overrides and tooling state
*.bicep.local
*.parameters.local.json

# Azure Developer CLI
.azure/

# Copilot / agent scratch files (out of scope for IaC)
.github/agents/
.github/copilot-instructions.md

# Workflows not related to Front Door WAF CI (kept locally, excluded from PR scope)
.github/workflows/backend-deploy.yml
.github/workflows/frontend-deploy.yml
.github/workflows/infra-deploy.yml

# Terraform (in case added later)
.terraform/
*.tfstate
*.tfstate.*
.terraform.lock.hcl

# Misc
*.swp
*.swo
